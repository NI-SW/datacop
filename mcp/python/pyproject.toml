[project]
name = "datacop-mcp"
version = "2.0.0"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.8.0",
    "pymysql>=1.1.0",
    "bcrypt>=4.2.0",
    "PyJWT>=2.8.0",
]

[project.scripts]
datacop-mcp = "server:main"
